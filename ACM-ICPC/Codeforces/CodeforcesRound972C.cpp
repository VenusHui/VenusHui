/**
 * @platform: Codeforces
 * @problem: CodeforcesRound972C 
 * @version: Contest Version
 * @author: VenusHui
 * @date: 2024-09-21
 */
#include <bits/stdc++.h>
typedef long long ll;
typedef unsigned long long ull;
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int t;
    cin >> t;
    while (t--) {
        int n, m;
        cin >> n >> m;
        vector<int> p(n);
        for (int i = 0; i < n; i++) cin >> p[i];
        
    }

    return 0;
}
