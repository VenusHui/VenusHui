/**
 * 草稿可见性策略 —— 全站唯一一处定义。
 *
 * 本地 dev 可见（作者要预览未完成稿），`astro build` 与线上永远不可见。
 *
 * 取内容的每一处都必须经过它：列表、归档、标签、RSS、**详情页路由**、简历。
 * 之前详情页路由绕过这里直接查集合，草稿正文会被构建出来并写进 sitemap，
 * 而列表侧看起来是干净的 —— 所以新增取内容的入口时，务必复用本模块。
 */
export const includeDrafts = import.meta.env.DEV;
