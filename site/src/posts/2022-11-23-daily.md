---
title: 【Daily】11.23 Daily Coding
date: 2022-11-23
tags: [daily-coding, leetcode]
summary: Leetcode 1742 盒子中小球的最大数量
lang: zh
---

## Leetcode

### [1742. 盒子中小球的最大数量](https://leetcode.cn/problems/maximum-number-of-balls-in-a-box/)

> 题目描述 / 样例 / 数据范围见原题链接，以下为个人题解。

#### 题目分析

对于 `1` 到 `n` 的每一个小球直接进行模拟，最后选出数量最大的盒子的即可

#### 复杂度分析

- 时间复杂度：`O(nlogr)`， `r` 为 `highLimit` 的十进制位数
- 空间复杂度：`O(1)` ，数据范围为 `1e5` 最多会有第 `99999` 个小球用到第 `45` 个盒子

#### AC代码

```cpp
class Solution {
public:
    int countBalls(int lowLimit, int highLimit) {
        unordered_map<int, int> mp;
        for (int i = lowLimit; i <= highLimit; i++) {
            string s = to_string(i);
            int tmp = 0;
            for (auto& e : s) {
                tmp += e - '0';
            }
            mp[tmp]++;
        }
        int ans = 0;
        for (auto& i : mp) {
            ans = max(ans, i.second);
        }
        return ans;
    }
};
```
